= Rubualizations::Rails

rubualizations-rails is a ruby gem which converts rails ActiveRecords into data structures which can be used by (Google Visualizations) 
to create graphs in your web page.

See https://github.com/whatsthebeef/rubualizations


